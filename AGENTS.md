# AGENTS.md — L9 repo template

Operating stub for coding agents. Replace the mission section when deriving a product repo via `make rename`.

## Mission (fill in)

This repository is a thin L9 Python GitHub Template. Derived repos should state:

- what the package owns
- what it never owns (routing authority, peer dispatch, etc. — product-specific)

## Owns

- Example package under `src/` (rename with `make rename PKG=...`)
- Local verify surface: inventory, Cursor rule drift, lint, typecheck, tests
- CI sync from org pack (`make sync-ci`)
- Parametric Cursor rules (`plugin-config.yaml` → `make render-rules`)

## Never

- Peer URL dispatch or alternate transport contracts (product repos: follow your domain AGENTS)
- Re-authoring org-inheritable community health files
- Hand-editing `.github/workflows/*` copied from the pack — re-run `make sync-ci`
- Introducing FastAPI/OTel/`observability/`, `engine/`, `chassis/`, `domains/`, or golden-repo kitchen-sink layout
- Hand-editing generated `.cursor/rules/*.mdc` — edit templates or `plugin-config.yaml`

## Validation ladder

Run in this order. Do not skip a failing gate.

```bash
make inventory-check
make check-rules
make lint
make typecheck
make test
# or:
make verify
```

## Protected paths

| Path | Reason |
|------|--------|
| `uv.lock` | Generated by `uv lock` / `uv add` only |
| `pyproject.toml` | Re-lock after changes |
| `.github/workflows/*` | Org pack via `make sync-ci` |
| `.github/governance/*` | Org pack via `make sync-ci` |
| `.l9/ci-pin` | Org/Core SHA pins |
| `scripts/sync_ci_from_pack.py` | CI sync contract |
| `scripts/bootstrap_rename.py` | Rename tool identity must stay stable |

## CI (from Quantum-L9/.github via sync-ci)

| Workflow | Trigger | Role |
|----------|---------|------|
| `l9-lint-test.yml` | PR, push `main`, dispatch | ruff / mypy / pytest |
| `l9-analysis.yml` | PR, dispatch | semgrep + Core governance publish |

Pins live in `.l9/ci-pin`. Bump CI by updating the pin and running `make sync-ci`.

## Cursor rules

```bash
make render-rules
make check-rules
```

See [docs/PARAMETRIC_CURSOR_RULES.md](docs/PARAMETRIC_CURSOR_RULES.md).
