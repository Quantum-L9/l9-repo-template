name: PR Pipeline Gate

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  pr_pipeline:
    uses: Quantum-L9/l9-ci-core/.github/workflows/pr-pipeline.yml@v0.1.0
    with:
      python-version: "3.12"
      source-dir: "."
      test-dir: "tests/"
    secrets: inherit
