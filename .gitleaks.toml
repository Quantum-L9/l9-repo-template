# --- L9_META ---
# l9_schema: 1
# origin: l9-repo-template
# layer: security
# owner: platform
# status: active
# --- /L9_META ---
#
# Thin repository policy. Detection rules remain owned by Gitleaks defaults;
# central L9 CI may install and invoke the scanner. Add project-specific
# allowlists only for reviewed false positives, never as broad path exclusions.

title = "Quantum-L9 Python repository"

[extend]
useDefault = true
