<!-- L9_RENDERED
template: .cursor/rules/templates/10-domain-cartridge.mdc.template
template_sha256: 9eb1c85dbfa2d44905e1b0cc73c5ab54bb2e0df2b2f4dd386a7e491944406a52
config: plugin-config.yaml
config_sha256: 8c5d18391bd536b6f70b2bfcaf83f5d2e31fc58ec98934054211fd40bee9f2e1
render_id: 5799a566943825970d276914cc2a7b6d86e739d634e03532df36b5c912cbc981
/L9_RENDERED -->
---
description: "Domain cartridge rule — generated from plugin-config.yaml"
globs: [".cursor/rules/**", "plugin-config.yaml", "scripts/render_cursor_rules.py"]
alwaysApply: false
---
<!-- L9_META role: cursor_rule tags: [domain-cartridge, generated, l9-python-museum] version: 1.2.0 /L9_META -->

# Domain Cartridge Convention

> This file is generated from .cursor/rules/templates/*.mdc.template. Do not edit directly; edit the template or plugin-config.yaml.

## Core Law

Rules must be reusable across repos. Repo-specific values belong in `plugin-config.yaml`, not hardcoded inside `.mdc` files.

## Current Cartridge

- Repo: `l9-repo-template`
- Domain: `l9-python-museum`
- Plugin version: `1.2.0`
- Config SHA: `8c5d18391bd5`

## Template Variables

The renderer exposes scalar config keys directly and list keys as bullet/list/json variants:

- `- `uv.lock`
- `pyproject.toml`
- `.github/workflows/*.yml`
- `.github/governance/*.yaml`
- `.l9/ci-pin`
- `scripts/sync_ci_from_pack.py`
- `scripts/bootstrap_rename.py`
- `scripts/inventory_check.py`
- `scripts/repo_hygiene_audit.py`
- `Makefile`
- `tools/l9_repo/**`` / `- `uv.lock`
- `pyproject.toml`
- `.github/workflows/*.yml`
- `.github/governance/*.yaml`
- `.l9/ci-pin`
- `scripts/sync_ci_from_pack.py`
- `scripts/bootstrap_rename.py`
- `scripts/inventory_check.py`
- `scripts/repo_hygiene_audit.py`
- `Makefile`
- `tools/l9_repo/**`` / ``uv.lock`, `pyproject.toml`, `.github/workflows/*.yml`, `.github/governance/*.yaml`, `.l9/ci-pin`, `scripts/sync_ci_from_pack.py`, `scripts/bootstrap_rename.py`, `scripts/inventory_check.py`, `scripts/repo_hygiene_audit.py`, `Makefile`, `tools/l9_repo/**`` / `[
  "uv.lock",
  "pyproject.toml",
  ".github/workflows/*.yml",
  ".github/governance/*.yaml",
  ".l9/ci-pin",
  "scripts/sync_ci_from_pack.py",
  "scripts/bootstrap_rename.py",
  "scripts/inventory_check.py",
  "scripts/repo_hygiene_audit.py",
  "Makefile",
  "tools/l9_repo/**"
]`
- `- `git push --force`
- `rm -rf .venv`
- `make sync-ci`` / `- `git push --force`
- `rm -rf .venv`
- `make sync-ci`` / ``git push --force`, `rm -rf .venv`, `make sync-ci`` / `[
  "git push --force",
  "rm -rf .venv",
  "make sync-ci"
]`
- `- `make verify`
- `make check-rules`
- `make hygiene-check`` / `- `make verify`
- `make check-rules`
- `make hygiene-check`` / ``make verify`, `make check-rules`, `make hygiene-check`` / `[
  "make verify",
  "make check-rules",
  "make hygiene-check"
]`

## Graph Record

```json
{
  "capabilities": [
    "verify",
    "sync-ci",
    "rename",
    "render-rules",
    "hygiene-check",
    "birth-runner",
    "obs-optional"
  ],
  "data_governance_class": "internal",
  "maintenance_owner": "Quantum-L9/platform",
  "node_type": "plugin_install"
}
```
