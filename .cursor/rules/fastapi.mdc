<!-- L9_RENDERED
template: .cursor/rules/templates/fastapi.mdc.template
template_sha256: 6d2836a1c221ecf61b54dccc757ec204ce58800e2460dd267eba5c3b7b19e0be
config: plugin-config.yaml
config_sha256: 60bf21dd6142f9867e374b83a337d36b2d7997de2bdc83a50ebee6c4935f1f46
render_id: 03bf578c098ef2fcba3b7c86bf0bc18b8d2d379e87b7ec00deaa93b96940a1b8
/L9_RENDERED -->
---
description: "l9-repo-template FastAPI conventions — generated from museum cartridge"
globs: ["src/**/*.py"]
alwaysApply: false
---
<!-- L9_META role: cursor_rule tags: [fastapi, generated, quantum-l9-python-museum] version: 1.2.0 /L9_META -->

# FastAPI Conventions — l9-repo-template

> This file is generated from .cursor/rules/templates/*.mdc.template. Do not edit directly; edit the template or plugin-config.yaml.

## Route layer

- Prefer `APIRouter` with explicit `prefix` and `tags` as the app grows.
- Keep business logic out of route handlers; delegate to modules.
- Keep `GET /v1/health` (or `/healthz`) cheap and unauthenticated.
- This is **not** a Gate worker — do not require `GATE_URL` or constellation-node-sdk.

## Hygiene

- Do not use `print()` in `src/`; use logging if needed.
- Do not add OpenTelemetry Python packages by default; optional obs is compose-only.

## Runtime

- App entrypoint: `l9_example_pkg.app:app`
- Package name: `l9_example_pkg`
