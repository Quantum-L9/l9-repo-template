# Opt-in example — copy to repo root as .coderabbit.yaml if you use CodeRabbit.
# Not required for make verify or org CI.
language: en-US
reviews:
  profile: chill
  request_changes_workflow: false
  high_level_summary: true
  poem: false
  auto_review:
    enabled: true
    drafts: false
    base_branches: [main]
chat:
  auto_reply: false
