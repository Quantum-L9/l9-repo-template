# Golden Benchmark Harness

These cases test expected process behavior, not exact prose.

Add a regression case whenever a routing/promotion/learning defect is discovered. A canonical rule change must not be promoted without benchmark evidence.
